\section{Разработка интеллектуальной системы мониторинга рисков и обеспечения безопасности путешественников}
\label{sec:development}

В данном разделе подробно описывается процесс программной реализации спроектированной системы, приводятся расширенные примеры исходного кода ключевых модулей на языках Python и Java, а также демонстрируются результаты работы пользовательского интерфейса. Разработка велась с применением современных практик непрерывной интеграции, контейнеризации и автоматизированного тестирования, что позволило обеспечить высокую надежность и расширяемость всех компонентов интеллектуальной платформы.

\subsection{Разработка модуля сбора и первичной обработки данных}

Модуль Fetcher является входной точкой системы и реализован на языке Python 3.12 с использованием асинхронной парадигмы программирования. Основная сложность реализации данного модуля заключалась в необходимости обработки огромных массивов данных проекта GDELT, которые публикуются в виде сжатых CSV-файлов каждые 15 минут и могут содержать информацию о тысячах событий одновременно.

Для обеспечения высокой производительности и минимизации задержек был разработан асинхронный клиент на базе библиотеки `httpx`, позволяющий параллельно загружать архивы событий (Events) и упоминаний (Mentions). После загрузки данные передаются в конвейер обработки, реализованный с применением библиотеки `pandas`, которая обеспечивает векторизованную фильтрацию и очистку данных на низком уровне.

Ниже представлен фрагмент реализации асинхронного сервиса загрузки данных:

\begin{verbatim}
import asyncio
import httpx
import pandas as pd
from aio_pika import connect, Message

class GdeltFetcher:
    def __init__(self, rabbit_url: str):
        self.rabbit_url = rabbit_url
        self.base_url = "http://data.gdeltproject.org/gdeltv2/"

    async def fetch_latest_export(self, file_name: str):
        async with httpx.AsyncClient() as client:
            url = f"{self.base_url}{file_name}"
            response = await client.get(url)
            if response.status_code == 200:
                with open(f"/tmp/{file_name}", "wb") as f:
                    f.write(response.content)
                return True
        return False

    async def process_csv(self, path: str):
        df = pd.read_csv(path, sep='\t', header=None)
        # GoldsteinScale < -5.0 (критические инциденты)
        critical_events = df[df[30] < -5.0] 
        return critical_events[0].tolist()

    async def notify_analyzer(self, event_ids: list):
        connection = await connect(self.rabbit_url)
        async with connection:
            channel = await connection.channel()
            for eid in event_ids:
                msg = Message(str(eid).encode())
                await channel.default_exchange.publish(
                    msg, routing_key="events_queue"
                )
\end{verbatim}

Ключевые особенности реализации модуля сбора данных:
\begin{itemize}
    \item применение асинхронных контекстных менеджеров для эффективного управления сетевыми соединениями и ресурсами операционной системы;
    \item использование векторизованных операций библиотеки `pandas` для мгновенной фильтрации записей по пороговым значениям индекса Гольдштейна;
    \item реализация отказоустойчивой очереди сообщений на базе RabbitMQ, гарантирующей доставку идентификаторов событий в аналитический модуль;
    \item внедрение механизмов логирования и мониторинга состояния сетевых запросов для оперативного выявления сбоев на стороне источников данных;
    \item использование временных хранилищ для минимизации нагрузки на оперативную память при обработке файлов объемом в несколько сотен мегабайт.
\end{itemize}

Данный подход позволил сократить время от момента публикации данных в GDELT до начала их анализа нейронными сетями до 5-10 секунд, что является критически важным для систем экстренного оповещения.

\subsection{Программная реализация интеллектуального анализа на Python}

Модуль Analyzer представляет собой высокотехнологичный конвейер на базе FastAPI, объединяющий классические методы NLP и современные генеративные модели. Процесс анализа разделен на этапы извлечения контента, семантической классификации и суммаризации, что позволяет гибко настраивать параметры каждой модели в зависимости от типа входных данных.

Для классификации угроз используется модель DeBERTa-v3-large, настроенная на задачу Zero-shot Classification. Это позволяет системе распознавать новые типы инцидентов (например, специфические киберугрозы или новые виды природных аномалий) без необходимости формирования обучающей выборки и дообучения весов нейронной сети.

Особое внимание уделено интеграции с локальной языковой моделью через API Ollama. Это обеспечивает полную автономность системы и исключает зависимость от внешних провайдеров (таких как OpenAI или Anthropic), что критично для обеспечения конфиденциальности и снижения операционных затрат.

Листинг кода модуля анализа с применением трансформеров и LLM:

\begin{verbatim}
from transformers import pipeline
import httpx

class IntelligenceAnalyzer:
    def __init__(self, ollama_url: str):
        # Модель для Zero-Shot классификации
        model_name = "MoritzLaurer/DeBERTa-v3-large-mnli-fever-anli-ling"
        self.classifier = pipeline(
            "zero-shot-classification", 
            model=model_name
        )
        self.ollama_url = ollama_url
        self.labels = ["terrorism", "disaster", "protest", "medical"]

    async def analyze_text(self, text: str):
        # 1. Семантическая классификация категории
        res = self.classifier(text, self.labels, multi_label=False)
        
        # 2. Суммаризация через локальную LLM
        summary = await self.generate_llm_summary(text)
        
        return {
            "category": res['labels'][0],
            "confidence": res['scores'][0],
            "summary": summary
        }

    async def generate_llm_summary(self, content: str):
        instruct = "Summarize this news in one short sentence:"
        prompt = f"{instruct} {content[:1000]}"
        payload = {"model": "qwen2.5:7b", "prompt": prompt, "stream": False}
        async with httpx.AsyncClient() as client:
            url = f"{self.ollama_url}/api/generate"
            response = await client.post(url, json=payload)
            return response.json().get("response", "N/A")
\end{verbatim}

Программные решения, внедренные в аналитический модуль:
\begin{itemize}
    \item использование библиотеки `transformers` для реализации высокоточной семантической классификации текстов на естественном языке;
    \item интеграция с локальным сервером Ollama через REST API для выполнения ресурсоемких задач суммаризации на выделенных GPU-мощностях;
    \item ограничение входного контекста для LLM до 1000 символов с целью оптимизации времени инференса и предотвращения галлюцинаций модели;
    \item применение асинхронных вызовов к API нейросетевых моделей для обеспечения параллельной обработки нескольких инцидентов одновременно;
    \item реализация механизмов повторных попыток (Retry) при взаимодействии с вычислительными бэкендами машинного обучения.
\end{itemize}

\subsection{Реализация серверной логики и ГИС-компонентов на Java}

Ядро системы, отвечающее за хранение данных, управление пользователями и выполнение пространственных запросов, реализовано на Java 21 с использованием фреймворка Spring Boot 3.3. Ключевым технологическим преимуществом является использование Hibernate Spatial, что позволяет оперировать геометрическими объектами непосредственно в коде приложения.

Для реализации логики геофенсинга — поиска пользователей, находящихся в зоне риска — применяется специализированная структура данных PostGIS. Это позволяет выполнять сложные математические вычисления на стороне СУБД, возвращая в приложение только готовый результат.

Пример реализации Entity-класса события с поддержкой пространственных данных:

\begin{verbatim}
@Entity
@Table(name = "events")
@Data
public class EventEntity {
    @Id
    @GeneratedValue(strategy = GenerationType.UUID)
    private UUID id;

    private String title;
    
    @Column(columnDefinition = "TEXT")
    private String description;

    @Column(columnDefinition = "geometry(Geometry, 4326)")
    private Geometry impactArea;

    @Column(name = "created_at")
    private LocalDateTime createdAt;
}
\end{verbatim}

Бизнес-логика проверки безопасности маршрута реализована в сервисном слое. Ниже представлен фрагмент кода, отвечающий за пересечение маршрута пользователя с активными зонами угроз:

\begin{verbatim}
@Service
@RequiredArgsConstructor
public class RiskAssessmentService {
    private final EventRepository eventRepository;

    public List<RiskNotificationDto> checkRouteSafety(Geometry route) {
        // Выполнение ST_Intersects в базе данных
        var events = eventRepository.findIntersecting(route);
        
        return events.stream()
            .map(e -> new RiskNotificationDto(e.getId(), e.getTitle()))
            .toList();
    }
}
\end{verbatim}

Технические особенности реализации серверной части:
\begin{itemize}
    \item использование библиотеки JTS (Java Topology Suite) для выполнения точных геометрических вычислений на стороне приложения;
    \item применение аннотаций JPA и Hibernate Spatial для автоматического маппинга географических координат в типы данных PostGIS;
    \item реализация кастомных репозиториев с использованием Native SQL для оптимизации сложных пространственных операций пересечения и буферизации;
    \item использование паттерна DTO (Data Transfer Object) для безопасной и эффективной передачи данных между бэкендом и фронтенд-приложением;
    \item внедрение механизмов кэширования результатов гео-запросов для снижения нагрузки на дисковую подсистему базы данных при частом обновлении координат.
\end{itemize}

\subsection{Реализация системы многоканальных уведомлений}

Для обеспечения гарантированной доставки информации об угрозах в системе реализована многоканальная рассылка, включающая Push-уведомления и электронную почту. Сервис уведомлений на Java использует `Spring Mail` для взаимодействия с SMTP-сервером и формирования HTML-шаблонов писем.

Пример реализации сервиса отправки email-оповещений:

\begin{verbatim}
@Service
@RequiredArgsConstructor
public class EmailNotificationService {
    private final JavaMailSender mailSender;
    private final TemplateEngine templateEngine;

    public void sendAlert(String to, EventEntity event) {
        Context context = new Context();
        context.setVariable("event", event);
        String body = templateEngine.process("risk-alert", context);
        
        MimeMessage message = mailSender.createMimeMessage();
        MimeMessageHelper helper = new MimeMessageHelper(message);
        helper.setTo(to);
        helper.setSubject("Security Alert: " + event.getTitle());
        helper.setText(body, true);
        
        mailSender.send(message);
    }
}
\end{verbatim}

Важным этапом разработки стала верстка шаблонов уведомлений, которые должны быть читаемы на любых устройствах. На рисунке \ref{fig:email_notification} представлено окно с email-сообщением об опасности, которое получает пользователь при вхождении в зону риска.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.9\textwidth]{picture/email_notification_view.png}
    \caption{Окно с email-уведомлением о критической угрозе безопасности}
    \label{fig:email_notification}
\end{figure}

\subsection{Разработка клиентского интерфейса и ГИС-компонентов}

Фронтенд-приложение построено на базе React и TypeScript. Для интеграции карты в React-компоненты используется библиотека `react-map-gl`, которая оборачивает вызовы MapLibre GL JS.

Пример инициализации карты и добавления слоя зон риска:

\begin{verbatim}
const RiskMap = ({ events }) => {
  return (
    <Map
      initialViewState={{ longitude: 0, latitude: 0, zoom: 2 }}
      mapStyle="https://demotiles.maplibre.org/style.json"
    >
      <Source id="risk-zones" type="geojson" data={events}>
        <Layer
          id="zone-fill"
          type="fill"
          paint={{ 'fill-color': '#ff0000', 'fill-opacity': 0.4 }}
        />
      </Source>
    </Map>
  );
};
\end{verbatim}

На рисунке \ref{fig:main_map_view} представлен главный интерфейс системы в режиме реального времени.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{picture/main_map_interface.png}
    \caption{Главный экран системы с визуализацией активных инцидентов и зон риска}
    \label{fig:main_map_view}
\end{figure}

Пользователь имеет возможность не только наблюдать текущую ситуацию, но и проводить аналитическую работу. На рисунке \ref{fig:route_analysis} показан процесс анализа планируемого пути путешественника.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{picture/route_analysis_view.png}
    \caption{Интерфейс анализа маршрута и отображение предупреждений о безопасности}
    \label{fig:route_analysis}
\end{figure}

Важнейшим компонентом системы является модуль доставки Push-уведомлений. Пример такого уведомления представлен на рисунке \ref{fig:mobile_notifications}.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.6\textwidth]{picture/mobile_notif_example.png}
    \caption{Пример Push-уведомления о критической угрозе на мобильном устройстве}
    \label{fig:mobile_notifications}
\end{figure}

\subsection{Выводы по реализации}

В результате дипломного проектирования был разработан и успешно протестирован полноценный программный комплекс интеллектуального мониторинга рисков, который полностью отвечает всем функциональным требованиям. В ходе реализации были решены сложные инженерные задачи, связанные с обработкой больших данных и интеграцией нейросетевых моделей в реальный бизнес-процесс.

Основные результаты этапа разработки:
\begin{itemize}
    \item реализован высокопроизводительный асинхронный модуль сбора данных на Python, способный обрабатывать поток новостей из проекта GDELT в реальном времени;
    \item разработан интеллектуальный аналитический конвейер, использующий модели DeBERTa-v3 для классификации и LLM Qwen 2.5 для автоматической суммаризации инцидентов;
    \item создана надежная серверная часть на Java со встроенной поддержкой геопространственных данных и механизмов асинхронного геофенсинга;
    \item внедрена база данных PostgreSQL с расширением PostGIS, обеспечивающая высокую скорость выполнения пространственных запросов на пересечение маршрутов;
    \item разработан современный клиентский интерфейс на базе WebGL, обеспечивающий плавную визуализацию глобальной карты рисков;
    \item реализована система мгновенных Push-уведомлений и email-рассылок, гарантирующая доставку информации о безопасности конечному пользователю.
\end{itemize}

Все компоненты системы интегрированы в рамках единой событийно-ориентированной микросервисной архитектуры, что обеспечивает высокий потенциал для дальнейшего горизонтального масштабирования и внедрения новых прогностических модулей.
